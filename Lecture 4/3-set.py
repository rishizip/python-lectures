# set in python
# each element in the set must be unique & immutable
# unorderd 
# repeated elements only store once
# sets are mmutable but sets elements are immutable

nums = {1, 2, 2, 3, 4, "world", "hello", 2.3, 8, 10, 8, 10}
print(type(nums))
print(nums)

collection = {} # -> this is for empty dictionary
print(type(collection))

# empty set
empty = set()
print(type(empty))